# AquaGuard AI Backend

Minimal Flask backend placeholder for local API testing.

```bash
pip install -r ../requirements.txt
python app.py
```

The production Colab/ngrok API can be added here as `aquaguard_colab_api.py` when available.
